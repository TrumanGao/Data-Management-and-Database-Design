# Scenario: You receive an unnormalized table containing order, product, and customer details.
# Tasks:
#
# Normalize the table up to 3rd Normal Form (3NF).
# Create the normalized tables with appropriate constraints.
# Write SQL joins to reconstruct the original data.
# Deliverable: ER diagram, SQL scripts, and a 1-page explanation on the impact of normalization.